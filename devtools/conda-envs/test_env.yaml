name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
  - numpy
  - rdkit
  - openbabel
  - MDAnalysis
  - biopython
  - ipywidgets

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

