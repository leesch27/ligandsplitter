name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
  - numpy==1.26.4
  - rdkit
  - openbabel
  - MDAnalysis
  - biopython
  - ipywidgets

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - rcsbsearchapi

    # Pip-only installs
  #- pip:
  #  - codecov

