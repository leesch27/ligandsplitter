name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
  - numpy==1.26.4
  - pandas
  - rdkit
  - openbabel
  - MDAnalysis
  - biopython
  - ipywidgets
  - scikit-learn
  - py-xgboost

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
  #  - codecov
    - rcsb-api

